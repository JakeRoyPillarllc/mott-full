%body#press
  .container
    .row
      .col-lg-12.col-xs-12
        .main_container
          .inner_container
            .menu
              = render :partial => "main_menu"
            .content_box.main
              .prev
                PREV
              .next
                NEXT
              .mainner_box
                #bh_slide_1
                  .main_box_press.wrapper-parent.lobby_img
                    = image_tag "press.png"
                  .main_box_press.wrapper-parent.lobby_img
                    = image_tag "press.png"
                  .ws_shadow
            .footer
              .footer_left
                %a{href: legal_homes_path} Legal
              .footer_right Alfa Development © 2014
            .footer_logo
              = image_tag "199-motto_logo.png"

:javascript

// Custom Slider
  var cur_slide=0;
  var bh_totel_slide = $('.main_box_press').length;
  var bh_neg_total = 0-bh_totel_slide;
  
  $('.next').click(function(e) {
    $('.prev').show();
    bh_slide('right');
    e.preventDefault();
  });
  
  $('.prev').click(function(e) {
    $('.next').show();
    bh_slide('left');
    e.preventDefault();
  });
  
  function bh_slide(dir) {
    var  slide=0 
    var left_css
    if(dir == 'left') 
      cur_slide++;
    else
      cur_slide--;
      slide=cur_slide;
      left_css = slide*1105;
      left_css += 'px';
    $('#bh_slide_1').css('margin-left',left_css);
    if( (cur_slide) == (bh_neg_total+1) )
      $('.next').hide();
    if( (cur_slide) == 0 ) 
      $('.prev').hide();
  }
